const messageController = require('./message.controller');
const roomController = require('./room.controller');
const userController = require('./user.controller');

module.exports = {
    messageController, roomController, userController
}